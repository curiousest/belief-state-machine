# Order of operations

1. (generate_text) Generate text a bunch of times from prompt data
2. (create_unlabelled_from_text) Create unlabelled dataset from all the data generated
3. (label_dataset) Run the labelling UI to label the data
4. evaluate...?